/**
 * Created by bips on 2017/09/21
 */

const initialState = {

};

export default function homeReducer(state = initialState, action = {}) {

    switch (action.type) {
        
        default:
            return state;
    }
}

